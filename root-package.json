{
  "name": "santa-cruz-archive",
  "version": "1.0.0",
  "description": "Santa Cruz Photographic Archive with React frontend and Express backend",
  "main": "index.js",
  "scripts": {
    "client": "cd src && npm start",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd .. && cd src && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
} 